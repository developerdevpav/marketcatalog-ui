import {SortCatalogListPipe} from './sort-catalog-list.pipe';

describe('SortCatalogListPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCatalogListPipe();
    expect(pipe).toBeTruthy();
  });
});
